// Custom React hooks
import { useEffect } from 'react';

export function useInteractiveCard() {
    useEffect(() => {
        const cards = document.querySelectorAll('.interactive-card');
        // Example effect setup here
    }, []);
}
